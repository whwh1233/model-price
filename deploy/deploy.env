# Deployment configuration — edit before running deploy.sh
# Backend port (change if conflicts with existing services)
BACKEND_PORT=8090

# Domain
DOMAIN=price.pubwhere.cn

# Project directory on VPS
APP_DIR=/root/model-price

# uv binary path
UV_BIN=/root/.local/bin/uv
